A fast and easy way to quickly prepare AXIS Scale database for company work.

Load up a "user key" list in UI, using a CSV file - Set up a table in Excel like this:
[ ID | Name of the user ]

and load it up - it caches so you only have to load it one time (or more if you plan to update it).

Then, load the database and your report is ready for all kinds of company work.
